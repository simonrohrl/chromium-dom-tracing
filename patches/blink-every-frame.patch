diff --git a/cc/scheduler/scheduler.cc b/cc/scheduler/scheduler.cc
index 9ccc5e622a..d8404bb7bb 100644
--- a/cc/scheduler/scheduler.cc
+++ b/cc/scheduler/scheduler.cc
@@ -13,6 +13,7 @@
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
+#include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/rand_util.h"
 #include "base/task/delay_policy.h"
@@ -385,6 +386,7 @@ void Scheduler::OnBeginFrameSourcePausedChanged(bool paused) {
 // If the scheduler is busy, we queue the BeginFrame to be handled later as
 // a BeginRetroFrame.
 bool Scheduler::OnBeginFrameDerivedImpl(const viz::BeginFrameArgs& args) {
+  LOG(INFO) << "[VSync->BeginFrameSource] OnBeginFrameDerivedImpl called"; 
   TRACE_EVENT1("cc,benchmark", "Scheduler::BeginFrame", "args", args.AsValue());
 
   // If the begin frame interval is different than last frame and bigger than
@@ -403,6 +405,7 @@ bool Scheduler::OnBeginFrameDerivedImpl(const viz::BeginFrameArgs& args) {
 
   // Drop the BeginFrame if we don't need one.
   if (!state_machine_.BeginFrameNeeded()) {
+    LOG(INFO) << "[BeginFrame BLOCKED] BeginFrameNeeded() returned false - dropping frame";
     TRACE_EVENT_INSTANT0("cc", "Scheduler::BeginFrameDropped",
                          TRACE_EVENT_SCOPE_THREAD);
     // Since we don't use the BeginFrame, we may later receive the same
@@ -701,6 +704,7 @@ void Scheduler::SendDidNotProduceFrame(const viz::BeginFrameArgs& args,
 // any asynchronous animation and scroll/pinch updates.
 void Scheduler::BeginImplFrame(const viz::BeginFrameArgs& args,
                                base::TimeTicks now) {
+  LOG(INFO) << "BeginImplFrame called";
   DCHECK_EQ(state_machine_.begin_impl_frame_state(),
             SchedulerStateMachine::BeginImplFrameState::IDLE);
   DCHECK(!begin_impl_frame_deadline_timer_.IsRunning());
@@ -934,6 +938,7 @@ void Scheduler::ProcessScheduledActions() {
       case SchedulerStateMachine::Action::NONE:
         break;
       case SchedulerStateMachine::Action::SEND_BEGIN_MAIN_FRAME:
+        LOG(INFO) << "Scheduler processing SEND_BEGIN_MAIN_FRAME action";
         compositor_timing_history_->WillBeginMainFrame(begin_main_frame_args_);
         compositor_frame_reporting_controller_->WillBeginMainFrame(
             begin_main_frame_args_);
diff --git a/cc/scheduler/scheduler_state_machine.cc b/cc/scheduler/scheduler_state_machine.cc
index 8cfa825188..baed98c429 100644
--- a/cc/scheduler/scheduler_state_machine.cc
+++ b/cc/scheduler/scheduler_state_machine.cc
@@ -9,6 +9,7 @@
 #include "base/check_op.h"
 #include "base/feature_list.h"
 #include "base/format_macros.h"
+#include "base/logging.h"
 #include "base/notreached.h"
 #include "base/time/time.h"
 #include "base/trace_event/trace_event.h"
@@ -580,21 +581,29 @@ bool SchedulerStateMachine::CouldSendBeginMainFrame() const {
     return false;
 
   // We can not perform commits if we are not visible.
-  if (!visible_)
+  if (!visible_) {
+    LOG(INFO) << "[CouldSendBeginMainFrame BLOCKED] visible_=false";
     return false;
+  }
 
   // There are no BeginImplFrames while viz::BeginFrameSource is paused,
   // so should also stop BeginMainFrames.
-  if (begin_frame_source_paused_)
+  if (begin_frame_source_paused_) {
+    LOG(INFO) << "[CouldSendBeginMainFrame BLOCKED] begin_frame_source_paused_=true";
     return false;
+  }
 
   // Do not send begin main frame when it is deferred.
-  if (defer_begin_main_frame_)
+  if (defer_begin_main_frame_) {
+    LOG(INFO) << "[CouldSendBeginMainFrame BLOCKED] defer_begin_main_frame_=true";
     return false;
+  }
 
   // Do not send begin main frames if we want to pause rendering.
-  if (pause_rendering_)
+  if (pause_rendering_) {
+    LOG(INFO) << "[CouldSendBeginMainFrame BLOCKED] pause_rendering_=true";
     return false;
+  }
 
   return true;
 }
@@ -615,10 +624,12 @@ bool SchedulerStateMachine::ShouldSendBeginMainFrame() const {
     case BeginMainFrameState::IDLE:
       break;
     case BeginMainFrameState::SENT:
+      LOG(INFO) << "[ShouldSendBeginMainFrame BLOCKED] begin_main_frame_state_=SENT (already sent)";
       return false;
     case BeginMainFrameState::READY_TO_COMMIT:
       if (!settings_.main_frame_before_commit_enabled ||
           next_begin_main_frame_state_ != BeginMainFrameState::IDLE) {
+        LOG(INFO) << "[ShouldSendBeginMainFrame BLOCKED] begin_main_frame_state_=READY_TO_COMMIT";
         return false;
       }
       break;
@@ -802,8 +813,10 @@ bool SchedulerStateMachine::ShouldInvalidateLayerTreeFrameSink() const {
 }
 
 SchedulerStateMachine::Action SchedulerStateMachine::NextAction() const {
-  if (ShouldSendBeginMainFrame())
+  if (ShouldSendBeginMainFrame()) {
+    LOG(INFO) << "Impl Sends BeginMainFrame";
     return Action::SEND_BEGIN_MAIN_FRAME;
+  }
   if (ShouldRunPostCommit())
     return Action::POST_COMMIT;
   if (ShouldActivateSyncTree())
@@ -1389,6 +1402,8 @@ void SchedulerStateMachine::OnBeginImplFrame(const viz::BeginFrameArgs& args) {
   did_invalidate_layer_tree_frame_sink_ = false;
   did_perform_impl_side_invalidation_ = false;
   waiting_for_scroll_event_ = false;
+  needs_begin_main_frame_ = true;
+  LOG(INFO) << "BeginFrame Impl";
 }
 
 void SchedulerStateMachine::OnBeginImplFrameDeadline() {
diff --git a/cc/trees/layer_tree_host.cc b/cc/trees/layer_tree_host.cc
index 6bfcfba051..a426006d02 100644
--- a/cc/trees/layer_tree_host.cc
+++ b/cc/trees/layer_tree_host.cc
@@ -391,6 +391,7 @@ void LayerTreeHost::BeginMainFrameNotExpectedUntil(base::TimeTicks time) {
 
 void LayerTreeHost::BeginMainFrame(const viz::BeginFrameArgs& args) {
   DCHECK(IsMainThread());
+  LOG(INFO) << "BeginMainFrame received in LayerTreeHost";
   client_->BeginMainFrame(args);
 }
 
diff --git a/cc/trees/proxy_impl.cc b/cc/trees/proxy_impl.cc
index 01c851053f..67e81cfd72 100644
--- a/cc/trees/proxy_impl.cc
+++ b/cc/trees/proxy_impl.cc
@@ -22,6 +22,7 @@
 #include "base/trace_event/traced_value.h"
 #include "base/trace_event/typed_macros.h"
 #include "base/types/optional_ref.h"
+#include "base/logging.h"
 #include "cc/base/devtools_instrumentation.h"
 #include "cc/base/features.h"
 #include "cc/benchmarks/benchmark_instrumentation.h"
@@ -740,6 +741,7 @@ void ProxyImpl::WillNotReceiveBeginFrame() {
 
 void ProxyImpl::ScheduledActionSendBeginMainFrame(
     const viz::BeginFrameArgs& args) {
+  LOG(INFO) << "ScheduledActionSendBeginMainFrame received in ProxyImpl";
   DCHECK(IsImplThread());
 
   benchmark_instrumentation::ScopedBeginFrameTask begin_frame_task(
diff --git a/cc/trees/proxy_main.cc b/cc/trees/proxy_main.cc
index 72990d3d71..ac975884ab 100644
--- a/cc/trees/proxy_main.cc
+++ b/cc/trees/proxy_main.cc
@@ -21,6 +21,7 @@
 #include "base/trace_event/trace_id_helper.h"
 #include "base/trace_event/traced_value.h"
 #include "base/types/optional_ref.h"
+#include "base/logging.h"
 #include "cc/base/completion_event.h"
 #include "cc/base/devtools_instrumentation.h"
 #include "cc/base/features.h"
@@ -138,6 +139,7 @@ void ProxyMain::DidCompletePageScaleAnimation() {
 
 void ProxyMain::BeginMainFrame(
     std::unique_ptr<BeginMainFrameAndCommitState> begin_main_frame_state) {
+  LOG(INFO) << "BeginMainFrame received in ProxyMain";
   DCHECK(IsMainThread());
   DCHECK_EQ(NO_PIPELINE_STAGE, current_pipeline_stage_);
   // Record the final status, subsampled. Use an RAII object as this function
diff --git a/third_party/blink/renderer/core/dom/node.cc b/third_party/blink/renderer/core/dom/node.cc
index c580313c33..3834912818 100644
--- a/third_party/blink/renderer/core/dom/node.cc
+++ b/third_party/blink/renderer/core/dom/node.cc
@@ -2685,12 +2685,12 @@ String Node::ToString() const {
   return builder.ReleaseString();
 }
 
-#if DCHECK_IS_ON()
-
 String Node::ToTreeStringForThis() const {
   return ToMarkedTreeString(this, "*");
 }
 
+#if DCHECK_IS_ON()
+
 String Node::ToFlatTreeStringForThis() const {
   return ToMarkedFlatTreeString(this, "*");
 }
@@ -2855,6 +2855,8 @@ static void AppendMarkedFlatTree(const String& base_indent,
   }
 }
 
+#endif
+
 String Node::ToMarkedTreeString(const Node* marked_node1,
                                 const char* marked_label1,
                                 const Node* marked_node2,
@@ -2872,6 +2874,8 @@ String Node::ToMarkedTreeString(const Node* marked_node1,
   return builder.ReleaseString();
 }
 
+#if DCHECK_IS_ON()
+
 String Node::ToMarkedFlatTreeString(const Node* marked_node1,
                                     const char* marked_label1,
                                     const Node* marked_node2,
@@ -3768,6 +3772,13 @@ void Node::Trace(Visitor* visitor) const {
 
 }  // namespace blink
 
+void ShowTree(const blink::Node* node) {
+  if (node)
+    LOG(INFO) << "\n" << node->ToTreeStringForThis().Utf8();
+  else
+    LOG(INFO) << "Cannot showTree for <null>";
+}
+
 #if DCHECK_IS_ON()
 
 void ShowNode(const blink::Node* node) {
@@ -3777,13 +3788,6 @@ void ShowNode(const blink::Node* node) {
     LOG(INFO) << "Cannot showNode for <null>";
 }
 
-void ShowTree(const blink::Node* node) {
-  if (node)
-    LOG(INFO) << "\n" << node->ToTreeStringForThis().Utf8();
-  else
-    LOG(INFO) << "Cannot showTree for <null>";
-}
-
 void ShowNodePath(const blink::Node* node) {
   if (node) {
     std::stringstream stream;
diff --git a/third_party/blink/renderer/core/dom/node.h b/third_party/blink/renderer/core/dom/node.h
index c637e01b20..276ac976a5 100644
--- a/third_party/blink/renderer/core/dom/node.h
+++ b/third_party/blink/renderer/core/dom/node.h
@@ -900,14 +900,14 @@ class CORE_EXPORT Node : public EventTarget {
 
   String ToString() const;
 
-#if DCHECK_IS_ON()
   String ToTreeStringForThis() const;
-  String ToFlatTreeStringForThis() const;
-  void PrintNodePathTo(std::ostream&) const;
   String ToMarkedTreeString(const Node* marked_node1,
                             const char* marked_label1,
                             const Node* marked_node2 = nullptr,
                             const char* marked_label2 = nullptr) const;
+#if DCHECK_IS_ON()
+  String ToFlatTreeStringForThis() const;
+  void PrintNodePathTo(std::ostream&) const;
   String ToMarkedFlatTreeString(const Node* marked_node1,
                                 const char* marked_label1,
                                 const Node* marked_node2 = nullptr,
diff --git a/third_party/blink/renderer/core/frame/web_frame_widget_impl.cc b/third_party/blink/renderer/core/frame/web_frame_widget_impl.cc
index 580ac00119..2cc944f117 100644
--- a/third_party/blink/renderer/core/frame/web_frame_widget_impl.cc
+++ b/third_party/blink/renderer/core/frame/web_frame_widget_impl.cc
@@ -38,6 +38,7 @@
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/callback_helpers.h"
+#include "base/logging.h"
 #include "base/functional/function_ref.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -2632,6 +2633,7 @@ void WebFrameWidgetImpl::OnCommitRequested() {
 }
 
 void WebFrameWidgetImpl::BeginMainFrame(const viz::BeginFrameArgs& args) {
+  LOG(INFO) << "BeginMainFrame received in Blink";
   base::TimeTicks last_frame_time = args.frame_time;
   TRACE_EVENT1("blink", "WebFrameWidgetImpl::BeginMainFrame", "frameTime",
                last_frame_time);
diff --git a/third_party/blink/renderer/platform/widget/compositing/layer_tree_view.cc b/third_party/blink/renderer/platform/widget/compositing/layer_tree_view.cc
index c32c90567f..69943776cf 100644
--- a/third_party/blink/renderer/platform/widget/compositing/layer_tree_view.cc
+++ b/third_party/blink/renderer/platform/widget/compositing/layer_tree_view.cc
@@ -273,6 +273,7 @@ void LayerTreeView::BeginMainFrame(const viz::BeginFrameArgs& args) {
   if (!delegate_)
     return;
   widget_scheduler_->WillBeginFrame(args);
+  LOG(INFO) << "BeginMainFrame received in LayerTreeView";
   delegate_->BeginMainFrame(args);
 }
 
